from django.apps import AppConfig


class MetadataRegistryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.metadata_registry"
    label = "metadata_registry"
    verbose_name = "Metadata Registry"

