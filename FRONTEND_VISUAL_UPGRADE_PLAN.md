# Airmap 前端视觉与交互升级方案

本轮前端升级采用“深色应用工作台 + 浅色数据工作区 + 全屏地图工作台”的混合体系，保留现有 Django API、Ant Design、Leaflet、业务模型和一级导航，不新增后端接口。

## 实施范围

- 统一应用壳层、主题令牌、状态色、间距、边框、阴影和响应式规则。
- 新增共享 `PageHeader`、`MetricStrip`、`StatusTag`、`SectionBar` 视觉组件。
- 重做数据、数据集、地图、服务、导入任务、治理和登录页面的视觉层级。
- 地图保留全屏布局、单景 JPG 预览、透明度和卷帘，不恢复多景图层栏。
- 不修改 Django、DuckDB、STAC、TiTiler、API 或数据结构。

## 设计约束

- 深海军蓝应用壳层，浅灰白内容区，地图保持全屏。
- 冷蓝色为主色，青色、绿色、琥珀色、红色仅用于业务状态。
- 不使用大面积渐变、发光球、装饰背景或营销式 Hero。
- 页面区块采用整面板和横向工具带，避免卡片嵌套。
- 支持 1366x768、1440x900、390x844，保证工具栏、详情栏和批量操作栏不重叠。

## 实施顺序

1. Ant Design Theme、全局 CSS 变量和应用壳层。
2. 共享页面头部、指标带、状态标签和区块工具栏。
3. 数据页、影像目录、数据集页面。
4. 地图浮层、详情栏和单景预览控制。
5. 服务页、导入任务 Drawer、治理 Drawer。
6. 登录页、移动端、无障碍和动效细节。
7. `npm run type-check`、`npm run build`、`antd lint` 和截图回归。
