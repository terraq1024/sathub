# 遥感数据如何管理？先从一块装满影像的硬盘说起

> 卫星遥感数据的获取成本在降，数据的"可用率"却一直上不去。问题往往出在管理这一环。本文从行业的真实困境讲起，介绍一套影像数据管理平台如何把散落各处的影像，变成整个团队随手可用的资产。

## 一、从一个熟悉的场景说起

每个做遥感的团队，几乎都有这样一块硬盘。

项目 A 的 SAR 数据在张三的电脑里，项目 B 的光学影像在部门服务器的 D 盘，去年采购的一批数据还躺在移动硬盘里，文件夹名叫"新建文件夹（3）"。谁手里有什么数据、覆盖哪些区域、什么时间拍的，没人说得清。

想找一景影像，先在群里问一圈；找到了，压缩包传过来，解压出十几个文件，还要装上专业软件才能看一眼。想给客户演示，只能临时抱佛脚；想发布成地图服务，得找后端同事排期。

数据花真金白银买回来了，大部分时间却躺在硬盘里睡觉。

## 二、数据都在，为什么用不起来

把这些现象拆开看，问题集中在五个方面：

**1. 找不到。** 数据散落在个人电脑、移动硬盘、部门服务器和网盘里，没有统一台账。项目一结束、人员一变动，数据的去向就成了悬案。

**2. 看不了。** 遥感影像动辄几百兆、几个 G，想预览一下范围和效果，得安装 ENVI、QGIS 这类专业工具。更多人干脆不看，凭文件名和经验判断。

**3. 管不住。** 同一景数据被不同的人下载、复制、转发，磁盘越来越大，有效资产却没有增加。文件版本、数据来源全靠口头约定，混乱只是时间问题。

**4. 共享难。** 给同事传数据靠微信和网盘，给客户交付数据靠裸压缩包。缺清单、缺校验、缺元数据说明，接收方拿到手还要再折腾一轮。

**5. 用不动。** 从"拿到影像"到"影像可用"之间隔着很多步：裁剪、波段组合、转格式、发布服务。每一步都需要专业人员和专业工具，一个简单的需求也要排队等排期。

这些问题的共同根源在于：数据始终以"文件"的形态散落各处，没有被管理成"资产"。

## 三、一套平台，把影像变成随手可用的资产

AirMap 是围绕这个问题建设的遥感影像统一管理平台。它做的事情可以概括成一条链路：

**接入 → 编目 → 检索 → 预览 → 组织 → 发布 → 处理 → 交付**

平台前端只有三个一级入口：数据、地图、服务。所有能力都挂在这条链路上，下面逐个展开。

### 1. 数据先聚起来：多种方式接入

对应痛点：找不到、管不住。

平台支持四种接入方式，覆盖数据进入团队的所有路径：

- **URL 链接导入**：把一批下载链接贴进输入框，后台任务自动下载、解压、入库；
- **压缩包与文件夹上传**：直接上传 ZIP、7Z 压缩包，或在浏览器里整目录拖拽上传；
- **目录扫描**：服务器本地目录、NAS 共享目录直接登记，平台执行全量与增量扫描。数据原位引用，留在原地，接入时不搬动、不复制；
- **轻量资产托管**：主数据留在 NAS，平台自动生成并托管缩略图、预览图和元数据文件，NAS 临时离线时，目录浏览和快速预览照样可用。

每次接入都是一个异步任务，总数、成功、失败、逐条明细全程可见，失败项可单独重试。

### 2. 元数据自动解析：不用再靠文件名猜

对应痛点：找不到。

影像的关键信息藏在配套 XML、文件名和栅格文件里。平台在入库时自动完成解析：

- 读取 XML 元数据，提取卫星、传感器、成像模式、极化方式、产品级别、分辨率、采集时间、四角坐标；
- XML 缺失时按命名规则从文件名解析，再回退到栅格文件本体，用 GDAL/rasterio 读取空间信息；
- 解析结果生成国际通用的 **STAC** 标准元数据，每景影像一个 STAC Item，同时写入 DuckDB 检索索引。

新增一种卫星产品，通过配置解析模板接入，改配置、不改代码。

### 3. 自动去重：一景数据只存一份

对应痛点：管不住。

平台以产品 ID 和文件指纹（SHA-256）为依据做跨用户去重。同一景数据无论谁上传、传几次，平台只保留一份物理文件，重复提交自动跳过并复用已有记录；指定新项目时只追加项目标签。磁盘不再被重复数据占满。

### 4. 检索与一张图：几秒钟锁定目标影像

对应痛点：找不到。

- **属性筛选**：卫星、传感器、产品级别、极化、时间范围、关键词，自由组合；
- **空间筛选**：在地图上框选矩形、绘制多边形，支持相交、包含、被包含三种空间关系，也可以直接粘贴 GeoJSON 范围；
- **行政区与标签**：影像入库后自动关联省、市、县行政区，可与分类、标签组合筛选；
- **保存查询**：常用筛选条件保存下来，一键重新执行，也能直接转成动态数据集。

检索结果在"一张图"上以 footprint 呈现，哪里有数据、哪里数据密集，一眼可见。列表、卡片、地图共享同一套筛选条件。

### 5. 在线预览与对比：打开浏览器就能看图

对应痛点：看不了。

- 点击任一 footprint，预览图直接叠加在地图上，透明度可调；
- 两景影像卷帘对比，拖动分割线前后比对；
- 最多四景按拍摄时间排列，多时相观察同一区域的变化；
- 波段组合生成快速预览；需要高清浏览和精细渲染时，后台再按需生成 COG（云优化 GeoTIFF），有缓存、可复用，生成失败也不影响检索和预览。

无需安装任何专业软件，浏览器里完成判读的第一步。

### 6. 数据集：把零散影像组织成业务单元

对应痛点：管不住、共享难。

- **静态数据集**：手动挑选一批影像，排序、启停成员，适合一个确定的业务主题；
- **动态数据集**：按查询条件自动圈定，符合条件的影像入库后自动纳入，适合"某区域最新 SAR 影像"这类持续更新的需求。

数据集同时是服务发布和交付的基本单元，一次组织，多处使用。

### 7. 服务发布：QGIS 里填个地址就能用

对应痛点：用不动。

在影像或数据集上点"发布服务"，平台自动完成 COG 准备、校验和注册，生成稳定的对外地址：

- 单景 XYZ / TileJSON 服务；
- 数据集镶嵌（MosaicJSON）服务，多景自动拼接；
- 兼容 OGC API Tiles 标准的发现与瓦片接口。

QGIS、ArcGIS 等客户端把地址填进 XYZ 图层即可加载，业务系统通过 API 直接调用。服务的发布、更新、下线、失败重试，都在服务页统一管理。

### 8. 在线处理：裁剪和波段组合不求人

对应痛点：用不动。

针对高频轻量需求，平台提供在线处理能力：按矩形或多边形裁剪、选择波段、执行简单波段表达式，输出 GeoTIFF 或 PNG。任务异步执行，完成后在线下载。能力有边界、有上限，客户端无法提交任意代码或路径。

### 9. 交付：有清单、可校验、能追溯

对应痛点：共享难。

- 影像加入数据篮，一键发起导出；
- 平台异步打包 **Manifest 清单 + STAC 元数据 + ZIP 全资产**，主数据、预览图、缩略图、元数据、日志分目录归档；
- 包内附带 `checksums.sha256` 校验文件，接收方可逐文件校验完整性；
- 资产通过带过期时间的签名 URL 访问，支持 HTTP Range 按需读取，不必下载整个文件。

正式交付走交付版本机制：冻结影像、元数据与服务引用，生成校验报告后才能交付，事后可回溯每一次交付的内容与状态。

### 10. 安全与审计：每一次操作都有底账

对应痛点：管不住、共享难。

账号登录，角色权限区分数据管理员、研发、售前、交付等岗位；API 访问使用令牌鉴权，令牌只存哈希、不存明文；登录、导入、解析、下载、发布、交付等关键操作全部写入审计日志，可按人、按时间、按对象追溯。

## 四、谁在用、怎么用

同一套平台，三类角色各取所需：

- **研发人员**：找数据、预览、裁剪、组合数据集、发布服务，全流程自助完成；
- **售前人员**：按区域和时间快速圈出可用数据，多时相对比做判读，演示直接调在线服务；
- **交付与管理**：家底有台账，交付有版本、有清单、有校验，出了问题能追溯。

## 五、写在最后

说清楚平台做什么，也要说清楚它做什么之外的事：它聚焦遥感影像的汇聚、编目、检索、预览、服务与交付，通用网盘、AI 解译、综合 GIS 分析不在它的边界内。技术选型上，平台大量采用开放标准（STAC、COG、XYZ / OGC API Tiles）和成熟开源组件（Django、DuckDB、TiTiler、Leaflet），数据随时可迁出，标准不锁定。

回到开头那块硬盘。遥感数据管理的第一步，是让所有影像有一个统一的家；第二步，是让每个人都能在几秒钟内找到它、看到它、用上它。遥感数据如何管理，答案就从这里开始。
