# Airmap 前端主题规范

## 视觉基调

- 以白色和浅灰蓝为页面基底，保持内容区明亮、安静。
- 主操作使用冷蓝色，地图和数据页面保持统一的操作反馈。
- 青绿色、琥珀色和红色只用于状态，不作为大面积装饰。
- 面板使用细边框和轻阴影，避免深色大面积背景、渐变、发光和装饰性图形。
- 页面区块优先使用矩形面板；地图浮层可使用适度圆角以保证悬浮层次。

## 颜色令牌

| 用途 | 颜色 | 说明 |
| --- | --- | --- |
| 主色 | `#3f6ff2` | 查询、提交、导航选中、地图重点操作 |
| 主色浅底 | `#f0f4ff` | 选中背景、hover 背景 |
| 成功/在线 | `#18a999` | 完成、在线、可用 |
| 警告/待处理 | `#e09b3d` | 等待、需更新、风险提示 |
| 错误/失败 | `#d95d5d` | 失败、禁止、危险操作 |
| 文字 | `#24324a` | 标题和主要内容 |
| 次要文字 | `#6e7d95` | 辅助说明、时间、元数据 |
| 页面背景 | `#f5f7fb` | 普通业务页面底色 |
| 面板背景 | `#ffffff` | 卡片、表格、抽屉和地图浮层 |
| 边框 | `#dce4f0` | 控件和面板边界 |

## 使用规则

- 一级导航使用白底，当前项使用主色文字、浅蓝背景和底部线条。
- 数据目录、服务和系统管理使用白色内容面和浅灰蓝页面背景。
- 指标块可以使用极浅的蓝、青绿、琥珀背景区分信息，但不使用色带和渐变。
- 地图保持地图主导，检索、底图和控制浮层使用半透明白底。
- 状态必须同时显示文字和颜色，不能只依赖颜色。
- 新增组件优先使用 Ant Design 主题令牌，不直接写新的颜色值。
- 本系统只面向桌面 Web，不为移动端增加专门布局。
