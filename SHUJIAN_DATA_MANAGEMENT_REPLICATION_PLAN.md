# 数简时空数字基础平台数据管理能力复刻方案

> 文档版本：v1.0  
> 调研日期：2026-08-25  
> 适用项目：`D:\code\airmap`  
> 文档性质：公开资料调研、产品范围定义、工程实施蓝图，不代表数简科技官方规格

## 1. 结论与实施原则

本项目不应只继续增加“影像上传”和“单景服务发布”按钮，而应升级为一套以数据资产为中心的时空数据管理平台。公开手册反复体现的核心不是某个算法，而是以下完整链路：

```text
用户数据空间
-> 文件和数据汇聚
-> 元数据标准化与编目
-> 数据对象管理
-> 图层逻辑编组
-> 服务组合与发布
-> 检索、浏览、订阅、下载和共享
-> 权限、计量、审计与运营监控
```

本次复刻遵循以下原则：

1. 复刻功能和工作流，不复制对方源代码、页面素材、品牌、文案或专有实现。
2. 以“数据管理”为主，算法模型、AI 解译、变化检测、几何精校正、无人机建模等算法本体不在范围内。
3. 保留算法任务的输入、输出、状态、血缘和编排接口，使未来可以接入第三方算法，但本项目不实现算法本身。
4. 采用“文件/数据对象 - 图层 - 服务”三层模型，不把物理文件、检索目录和对外服务混成一个对象。
5. Django 数据库是权限和业务关系的权威库；STAC 是标准目录表达；空间数据库负责空间查询；TiTiler 等组件负责服务渲染。
6. 优先复用成熟开源组件和标准协议，不自行实现栅格瓦片、矢量瓦片、STAC 查询或监控引擎。
7. 当前系统已有功能必须平滑迁移，已入库影像、STAC Item 和已发布服务不能因模型升级失效。

“完整复刻”的准确含义是：覆盖调研日期之前官方公开快速入门、产品介绍和公开教程中可确认的数据管理能力。未公开的内部功能、客户定制功能和专有引擎细节无法仅凭公开资料确认，不应伪装成已掌握规格。

## 2. 调研依据与可信度

### 2.1 一级资料：官方快速入门

主要入口：

- [数简科技资源中心 - 快速入门](https://shujiantech.com/resources/?tab=quickstart)

页面实际包含完整章节数据，确认的章节如下：

| 章节 | 公开条目 |
|---|---|
| 文件操作 | 我的数据、专有云映射、本地同步、无需发布直接浏览 |
| 数据发布与管理 | 批量属性提取、影像发布、矢量发布、高程发布、图层动态更新、三层模型、影像检索、无人机数据 |
| 数据处理与分析 | 动态投影、动态拉伸、边界提取、NDVI |
| 进阶 | 卫星数据自动化预处理、镶嵌数据集发布 |

本方案中标记为 `A` 的能力均可在上述官方快速入门中找到明确说明。

### 2.2 二级资料：官方产品页

- [数简时空数字基础平台](https://shujiantech.com/products/spatiotemporal)
- [数简影像基础平台](https://shujiantech.com/products/imagery)
- [数简矢量基础平台](https://shujiantech.com/products/vector)
- [数据基础设施、运营监控与访问计量](https://shujiantech.com/products/overview?target=structured-platform)

这些页面补充确认了多数据类型、分布式存储、数据订阅、结构化数据、血缘、版本、服务计量、运营监控和细粒度权限等能力。本方案中标记为 `B` 的能力来自这些页面。

### 2.3 三级资料：官方公开视频与外部文章

资源中心公开列出了以下主题：

- 文件操作
- 影像发布与管理
- Level 1A 数据自动处理发布
- 影像边界提取
- 同一影像的不同拉伸处理
- 数据属性提取展示
- 影像图层管理
- 数据检索
- 高程数据发布
- 影像镶嵌数据集发布
- 卫星遥感影像自动化预处理

公开视频用于验证功能操作语义，不作为专有技术实现依据。

### 2.4 证据等级

| 等级 | 定义 | 在本方案中的处理方式 |
|---|---|---|
| `A` | 官方快速入门有明确操作和步骤 | 作为必须复刻的产品行为 |
| `B` | 官方产品页明确宣称 | 纳入目标范围，验收口径由本项目独立定义 |
| `C` | 为形成完整数据管理闭环所需的工程补充 | 明确标注为本项目设计，不声称是对方原有能力 |
| `X` | 算法或专有引擎能力 | 本项目排除或只预留接口 |

## 3. 官方平台能力模型

### 3.1 数据空间和文件管理

官方“我的数据”本质上是每个用户独立的数据云盘，公开手册确认以下功能：

- 用户独立数据空间。`A`
- 文件夹创建、重命名、移动、复制和删除。`A/B`
- 文件上传、下载、在线解压和分类归档。`A`
- 本地磁盘与平台空间映射、双向同步。`A`
- 团队共享目录与协作。`A`
- 文件属性查看。`A`
- 原始数据、中间数据和成果数据统一管理。`B`
- NAS、SAN、DFS、OBS、本地文件和对象存储混合挂载。`B`
- “物理分散、逻辑集中”的统一目录。`A/B`

复刻要求：

1. 引入 `DataSpace`，至少支持个人空间、组织空间、项目空间和共享空间。
2. 引入虚拟目录树，目录节点不能等同于服务器真实路径。
3. 引入存储端点抽象，支持本地磁盘、NAS/SMB、SFTP/WebDAV 和 S3 兼容对象存储逐步接入。
4. 文件移动只改变逻辑目录时不搬动物理大文件；跨存储端点移动采用异步任务。
5. 以 SHA-256、大小和业务标识组合管理物理 Blob，避免同一文件重复占用空间。
6. 所有文件访问通过对象 ID 和权限校验，禁止前端提交服务器绝对路径。
7. 首期不开发专用 Windows 盘符客户端，先提供 WebDAV 或 SFTP 映射；后续再评估桌面同步客户端。

### 3.2 多源数据汇聚

官方产品体系覆盖：

- 多光谱、高光谱、红外、SAR、DEM/DSM、气象雷达等栅格数据。`B`
- SHP、GDB、KML、CSV、GeoJSON 等矢量数据。`A/B`
- Excel、PostgreSQL、MySQL、MSSQL 等结构化数据。`B`
- 轨迹、传感器、网格和物联网流式数据。`B`
- 三维模型、多媒体、兴趣点和名址数据。`B`
- HTTP/SFTP/WebDAV、目录、文件、数据库和接口集成。`B`

复刻范围按优先级收敛为：

| 优先级 | 数据类型 | 接入方式 |
|---|---|---|
| P0 | 遥感影像、DEM/DSM | ZIP/7Z、文件夹、URL、服务器目录登记 |
| P1 | 矢量数据 | ZIP、SHP 文件组、GeoJSON、KML、GPKG；GDB 视 GDAL 环境支持 |
| P1 | 结构化数据 | CSV、XLSX、PostgreSQL 只读连接 |
| P2 | 对象存储和 NAS | 存储端点扫描、增量同步、断点续传 |
| P3 | 轨迹/传感器/网格流 | HTTP API、Webhook、批量时序导入 |
| P3 | 三维和多媒体 | 文件资产管理、元数据和预览，不开发三维算法 |

所有接入统一进入 `IngestionJob -> IngestionItem -> DataObject` 流程，并具备校验、解析、入库、失败重试、取消和审计状态。

### 3.3 元数据、数据字典和编目

官方快速入门明确支持：

- 从文件名自动解析时间、卫星、分辨率等字段。`A`
- 从 XML 自动读取元数据，规则可配置。`A`
- 从 JSON 结合表读取指定元数据。`A`
- 手工批量录入同批数据属性。`A`
- 自定义扩展元数据字段。`A`
- 产品类型四级分类和组织自定义类目。`A`
- 发布前预览和修正属性。`A`

本项目应增加：

```text
MetadataSchema
MetadataField
MetadataMappingRule
MetadataExtractionProfile
CategoryNode
Tag
DataObjectMetadataValue
```

关键行为：

1. 元数据方案按数据类型、平台/卫星、供应商和产品级别选择。
2. 解析优先级保持 `sidecar > raster/vector header > filename > manual`，每个值记录来源、置信度和原始值。
3. 字段支持字符串、整数、小数、布尔、日期时间、枚举、数组、JSON、几何和单位。
4. 元数据规则可版本化；规则更新后可对选中数据批量重解析，但不能静默覆盖人工确认值。
5. 批量编辑先预览差异，再提交后台任务，并生成审计记录。
6. 支持缺失字段、冲突字段、异常范围和空间参考缺失等质量规则。
7. STAC Common、SAR、EO、Projection、Raster 等扩展字段与内部元数据建立稳定映射。

### 3.4 数据对象、图层和服务三层组织

这是官方快速入门中最重要的领域模型。`A`

#### 数据对象

- 管理原始文件和同景资产组。
- 保持原始格式和完整性。
- 记录位置、元数据、版本、质量、权限、来源和血缘。
- 一景影像、一份矢量数据集或一个 DEM 数据集都是数据对象。

#### 图层

- 图层是数据对象的逻辑分组，不是文件副本。
- 可显式选择成员，也可使用卫星、传感器、时间、空间范围、分类和标签等规则动态选取成员。
- 图层成员可排序、置顶、置底、启用、禁用或移除。
- 图层修改后无需重新上传原始数据。
- 图层保存默认坐标系、样式、透明度、波段和显示范围。

#### 服务

- 服务定义“哪些图层以何种协议、样式和权限对外提供”。
- 一个服务可包含一个或多个图层。
- 一个图层可被多个服务复用。
- 服务地址稳定，成员或样式调整不改变 URL。
- 上线、下线、重新发布和动态更新不删除原始数据。

当前项目只有 `ImageryRecord -> ImageryService`，缺少独立图层层级。必须新增 `DataLayer`、`LayerMembership`、`LayerRule` 和 `ServiceLayerBinding`，否则无法复刻官方的动态编组和多层服务。

### 3.5 数据检索和一张图

官方明确支持的查询维度：

- 全局关键词和文件名。`A/B`
- 数据 ID、产品类型、坐标系、卫星、负载/传感器。`A`
- 拍摄时间和时间范围。`A`
- 分辨率、缩略图和标签分类。`A/B`
- 矩形、多边形框选。`A`
- 行政区划。`A`
- 用户上传矢量范围。`A`
- 自定义坐标范围。`A`
- POI/地名地址。`B`
- 空间关系、光谱及属性特征过滤、空间统计。`B`
- 结果排序、分页、列表和缩略图。`A`
- 数据服务结果的时间序列展示。`A`
- 卷帘和多时相对比。`A`

本项目查询模型统一为：

```text
关键词 + 数据类型 + 分类 + 标签 + 时间 + 空间 + 业务属性 + 质量 + 服务状态
```

地图检索必须支持当前视口、矩形、多边形、行政区、上传 GeoJSON/SHP ZIP 和坐标输入。空间关系首期支持 `intersects`、`contains` 和 `within`。查询条件可保存为“检索方案”，并可一键转为动态图层或数据订阅。

### 3.6 数据浏览和可视化

官方手册强调数据无需先发布即可浏览。`A`

目标功能：

- 影像、DEM、矢量和结构化数据统一预览入口。
- 原始 JPG/缩略图快速预览。
- COG 全分辨率地图浏览。
- 矢量要素地图浏览和属性表。
- DEM 灰度、彩色分层和阴影样式；只做显示，不实现地形分析算法。
- 波段组合、线性拉伸、标准差拉伸、直方图均衡和 Gamma 参数化显示。
- 元数据、空间范围、资产、版本和服务关联信息侧栏。
- 双屏、卷帘、透明度和多时相对比。
- 测距、测面、坐标拾取和视图范围复制。

边界说明：动态拉伸、波段映射和投影仅属于渲染参数；NDVI、RPC 校正、几何精校正、镶嵌算法和 AI 解译不在本次算法排除范围内实现。可为这些处理定义任务接口和成果登记，但执行器由外部能力提供。

### 3.7 图层管理

必须实现：

- 新建静态图层和动态规则图层。
- 从检索结果创建图层。
- 图层成员缩略图和地图浏览。
- 成员排序、置顶、置底、启停和移除。
- 按卫星、传感器、时间、范围、分类、标签动态筛选成员。
- 图层版本和变更记录。
- 默认样式、坐标系、透明度和可见比例尺。
- 图层复制、归档和软删除。
- 图层被服务引用时的影响分析。
- 更新图层后服务地址保持不变。

“删除无效影像”默认解释为从图层移除或将数据对象标记为不可用，不直接物理删除唯一原始文件。物理清理由管理员在引用检查和保留期结束后执行。

### 3.8 服务发布和标准接口

官方手册和产品页确认影像、矢量、DEM、多图层组合、WMS/WMTS、PBF 以及多坐标系服务。`A/B`

本项目目标协议：

| 数据 | 首期协议 | 后续协议 | 推荐组件 |
|---|---|---|---|
| 单景栅格 | XYZ、TileJSON | WMS、WMTS、OGC API Tiles | TiTiler + OGC 网关 |
| 多景影像图层 | XYZ、TileJSON | WMS、WMTS、STAC Mosaic | TiTiler.PgSTAC 或 MosaicJSON |
| 矢量 | GeoJSON、OGC API Features | MVT/PBF、WMS/WFS | PostGIS + pygeoapi/GeoServer + Martin/pg_tileserv |
| DEM | XYZ、TileJSON | WMS、WMTS、高程查询 | TiTiler/Rio-Tiler |
| STAC | STAC Catalog 下载 | STAC API 1.0 | pgSTAC + stac-fastapi |

服务中心需要管理：

- 草稿、校验、准备、发布、在线、降级、离线、失败和归档状态。
- 服务图层组合和顺序。
- 默认样式与命名样式。
- 公开、登录可见、指定组织/用户和令牌访问。
- 稳定 URL、版本、缓存策略、最大缩放级别和数据范围。
- 健康检查、最近错误、调用量和响应耗时。
- 发布、重发、下线、克隆和归档。
- QGIS/ArcGIS/Web 调用示例和一键复制。
- 服务详情中的 Leaflet 在线预览。

当前 TiTiler COG 动态瓦片链路继续保留。发布时间主要用于 COG 准备、统计、校验和服务探测；不预生成全部 `{z}/{x}/{y}` 文件。

### 3.9 镶嵌数据集和多景服务

官方快速入门明确存在多景镶嵌数据集发布和多坐标系图层组合。`A`

本项目不实现复杂镶嵌算法，但必须实现数据管理层面的镶嵌集合：

- 从检索条件、目录或手工选择创建集合。
- 成员 footprint、时间、云量/质量、优先级和显示顺序。
- 规则化选择，如“最新优先”“低云量优先”“指定卫星优先”。
- 动态集合成员刷新。
- 多景服务发布、预览和稳定 URL。
- 成员差异、覆盖范围和时间范围统计。
- 不复制源数据，COG 在影像级复用。

### 3.10 数据订阅、分发和获取

官方产品页明确支持按类型、区域等需求订阅，检测更新并主动推送；快速入门还出现检索结果加入购物车采购或获取。`A/B`

内部平台先实现非商业化版本：

- 保存检索条件为订阅。
- 定时或入库事件触发匹配。
- 新数据站内通知、邮件、Webhook 推送。
- 去重通知和上次游标。
- 订阅暂停、恢复、立即试运行和历史命中。
- 将命中数据加入数据篮。
- 数据篮批量下载、Manifest、STAC ItemCollection 和临时分享链接。
- 大文件异步打包、过期时间和下载审计。
- 按组织、项目或外部系统配置固定分发规则。

充值、支付、商业订单和实际计费先列为可选模块，不进入内部系统首轮建设。

### 3.11 权限、安全和审计

官方公开能力包括 RBAC、用户/组织、属性和时空范围访问限制、SSO、SSL、水印和全行为记录。`B`

本项目继续使用 Django Auth，但从“所有登录用户看全部影像”升级为可配置策略：

- 组织、部门、用户组、角色和用户。
- 数据空间角色：所有者、管理员、编辑者、上传者、查看者。
- 数据对象、图层、服务和目录权限。
- 数据类型、分类、标签、空间范围、时间范围和属性条件策略。
- 下载、发布、编辑元数据、删除和授权单独控制。
- 公共服务、会话访问、API Token 和短期签名 URL。
- 登录、查询、浏览、下载、发布、授权、删除和管理操作审计。
- 文件哈希、来源、上传者、首次入库和所有变更追溯。
- 软删除、回收站、引用检查、保留期和管理员物理清理。
- 敏感预览和下载水印列为 P3。

### 3.12 访问计量和运营监控

官方产品页明确公开：按用户、服务、类型、区域记录成功/失败访问，支持按次、按量、按并发统计；Prometheus/Grafana 和 Elastic Stack 用于运维和业务监控。`B`

本项目应提供：

- 服务请求数、成功率、4xx/5xx、延迟分位数和流量。
- 用户查询、浏览、下载和发布次数。
- 数据新增量、存储量、重复率、解析成功率和预览覆盖率。
- 接入和发布任务积压、耗时和失败原因排行。
- 存储端点容量、可用性和扫描状态。
- Django、worker、TiTiler、PostgreSQL/PostGIS、Redis 和对象存储健康度。
- 告警规则、站内通知、邮件/Webhook；钉钉/短信作为适配器。
- 服务级配额和令牌限流。
- 运营大屏和按组织、用户、服务、数据类型钻取。

建议使用 Prometheus + Grafana + Loki/OpenSearch，不在 Django 中自制监控时序数据库。

### 3.13 版本、血缘和数据治理

官方结构化平台页面明确提到数据血缘，矢量平台页面明确提到版本追溯；元数据分类和统一标准也属于快速入门中的核心价值。`A/B`

本项目增加：

- 数据对象版本，不覆盖旧资产。
- `derived_from`、`replaces`、`part_of`、`generated_by` 血缘关系。
- 原始、中间、成果和基准数据生命周期类型。
- 数据质量状态和人工审核状态。
- 元数据完整率、空间参考、footprint、预览和资产可读性检查。
- 命名规范、元数据方案、分类树和单位字典版本。
- 重复业务景与重复物理文件分别判定。
- 数据修订、合并、拆分、归档和废弃。
- 数据责任人、维护组织、密级、使用限制和保留期限。

STAC 的 `derived_from`、`predecessor-version`、`successor-version`、Collection 和 Asset links 应与内部血缘同步。

### 3.14 流程编排和任务控制

虽然算法本体不在范围内，但数据管理流程仍需编排：

- 接入、校验、解压、扫描、解析、质检、入库、COG 准备、缩略图、STAC、索引和服务发布。
- 流程模板、版本、启停和部署。
- 定时、Webhook、文件到达和手工触发。
- 步骤状态、进度、日志、输入输出和耗时。
- 重试、超时、取消、补偿和死信处理。
- 并发限制、优先级、资源队列和幂等键。
- 流程运行历史和成果血缘。

首期使用代码定义的流程状态机；规模扩大后引入 Celery + Redis。BPMN 可视化编辑器属于 P3，不应在基础任务可靠性完善前优先开发。

## 4. 明确排除的算法范围

以下功能虽然出现在官方资料中，但属于算法或专有计算能力，本项目不复刻算法实现：

- Level 1A 到 DOM 的几何校正、精校正、匀光匀色和自动拼接。
- RPC 校正和控制点自动选取。
- 无人机航片空三、正射校正和稀疏纹理处理。
- 精准边界提取算法；可保留栅格有效区或 alpha mask 的通用边界读取。
- NDVI 及其他遥感指数计算。
- AI 目标识别、变化检测、地物分类和样本自动提取。
- 大模型 Agent、自主规划和自然语言算法调度。
- 轨道仿真、过境预测和观测任务优化。

允许实现的相邻能力：

- 算法目录、外部执行器注册和参数 Schema。
- 算法任务提交、状态、日志、权限、取消和重试。
- 输入数据选择和输出成果自动登记。
- 算法成果的血缘、元数据、预览和服务发布。
- 动态渲染参数，如波段选择、rescale、colormap、Gamma 和透明度。

## 5. 当前 Airmap 能力与差距

### 5.1 已有能力

- Django 登录、用户、组、权限和 Admin。
- ZIP、7Z、浏览器文件夹和 URL 影像接入。
- AIRSAT XML、文件名和栅格元数据解析。
- 影像 footprint、同名 JPG 预览和缩略图。
- 跨用户影像去重、接入任务和失败重试。
- Django 影像权威记录、DuckDB 查询投影和静态 STAC Item。
- 影像目录列表/卡片、影像一张图和筛选。
- 单景 COG 准备、TiTiler 动态 XYZ/TileJSON、服务状态和预览。

### 5.2 核心差距矩阵

| 能力 | 当前状态 | 目标 | 优先级 |
|---|---|---|---|
| 用户数据空间和虚拟目录 | 无 | 个人/组织/共享空间、文件操作、引用式移动 | P0 |
| 存储端点 | 固定本地目录 | 本地/NAS/S3/WebDAV/SFTP 抽象 | P1 |
| 元数据字典和规则配置 | 代码内固定解析器 | 可配置 Schema、字段、映射、批量编辑 | P0 |
| 四级数据分类和标签 | 项目标签较简单 | 分类树、标签体系、数据责任信息 | P0 |
| 文件-图层-服务三层模型 | 缺图层 | 静态/动态图层、多层服务 | P0 |
| 多景镶嵌服务 | 无 | 规则集合、成员排序、动态刷新 | P1 |
| 矢量数据管理 | 无 | 接入、PostGIS、预览、属性、服务 | P1 |
| DEM 专门管理 | 视作普通影像 | DEM 类型、样式和发布 | P1 |
| 结构化数据 | 无 | CSV/XLSX/数据库接入、Schema、CRUD | P2 |
| 多维空间检索 | bbox | 多边形、行政区、上传范围、空间关系 | P0 |
| 保存查询和数据订阅 | 无 | 入库事件匹配、通知、Webhook | P1 |
| 数据篮和批量分发 | 无 | Manifest/STAC/异步下载包 | P1 |
| STAC API | 静态文件 | pgSTAC + STAC API 搜索 | P1 |
| OGC 服务 | XYZ/TileJSON | WMS/WMTS、OGC API Features/Tiles | P1 |
| 细粒度权限 | 登录用户全量可见 | 组织/目录/数据/图层/服务/时空属性策略 | P1 |
| 版本和血缘 | 很少 | 版本、派生关系、质量和审核 | P1 |
| 使用计量 | 无 | 用户/令牌/服务/流量/延迟统计 | P2 |
| 运营监控 | 无 | 指标、日志、告警和看板 | P2 |
| 流程编排 | 两个轮询 worker | 可靠队列、流程模板、步骤和补偿 | P2 |
| 数据回收站和保留策略 | 无 | 软删除、引用检查、延迟清理 | P1 |

## 6. 目标信息架构

平台一级入口建议调整为：

1. `数据空间`
   - 我的数据、组织空间、共享给我、回收站、存储端点。
2. `数据管理`
   - 数据目录、影像、矢量、DEM、结构化数据、导入任务、元数据质检。
3. `时空一张图`
   - 全数据类型检索、地图选区、列表/缩略图、对比和数据篮。
4. `图层管理`
   - 静态图层、动态图层、镶嵌集合、成员和样式。
5. `服务管理`
   - 服务目录、发布任务、在线预览、接口、状态、令牌和调用统计。
6. `订阅分发`
   - 保存查询、订阅、推送、数据篮、导出和分享。
7. `运营中心`
   - 任务、服务、数据、存储和用户访问监控。
8. `系统管理`
   - 组织用户、权限、元数据字典、分类标签、存储、通知和审计。

普通用户只显示有权限的入口；系统管理主要进入 Django Admin 或独立管理页面。当前“影像管理、影像一张图、影像服务”可分别平滑迁入第 2、3、5 项。

## 7. 目标数据模型

### 7.1 空间和存储

```text
Organization
DataSpace
DataSpaceMembership
StorageEndpoint
DirectoryNode
AssetBlob
FileReference
```

- `AssetBlob` 表示物理内容，保存哈希、大小、存储端点和对象键。
- `FileReference` 表示逻辑目录中的文件名和引用。
- 同一 Blob 可以被多个目录或数据对象安全引用。
- 存储端点密钥只保存在服务端密钥管理中。

### 7.2 数据目录

```text
DataObject
DataObjectAsset
DataObjectVersion
DataLineageEdge
DataQualityCheck
CategoryNode
Tag
DataObjectTag
```

`DataObject.kind` 首期支持 `imagery_scene / imagery_mosaic / elevation / vector / table / document / media / model3d`。

现有 `ImageryRecord` 可以先作为影像扩展表，由 `DataObject` 一对一关联；不建议一次性重写全部影像代码。

### 7.3 元数据

```text
MetadataSchema
MetadataSchemaVersion
MetadataField
MetadataExtractionProfile
MetadataMappingRule
MetadataValue
MetadataConflict
```

固定高频查询字段放入类型扩展表；长尾自定义字段采用 JSONB，并对被配置为“可检索”的字段建立受控索引。

### 7.4 图层和服务

```text
DataLayer
LayerMembership
LayerRule
LayerStyle
ImageryService
ServiceLayerBinding
ServiceEndpoint
ServiceToken
ServicePublishJob
```

- 静态图层使用 `LayerMembership`。
- 动态图层使用版本化 `LayerRule`，并可生成成员快照。
- 服务通过 `ServiceLayerBinding` 组合多个图层并排序。
- 服务状态和对外 URL 继续由 Django 管理。

### 7.5 订阅、分发和运营

```text
SavedSearch
DataSubscription
SubscriptionMatch
NotificationEndpoint
DataBasket
DataBasketItem
ExportJob
ShareLink
UsageEvent
UsageAggregate
QuotaPolicy
AuditEvent
```

## 8. STAC 在目标平台中的位置

STAC 继续承担标准目录和交换，不承担权限和业务流程。

### 8.1 映射规则

| 内部对象 | STAC 对象 |
|---|---|
| 一景影像/单个 DEM | Item |
| 业务数据集或长期产品系列 | Collection |
| 原始、COG、预览、元数据 | Asset |
| 数据目录根 | Catalog，主要用于静态导出 |
| 图层 | 不强制映射；动态规则写入内部 API |
| 服务 | Item/Collection Asset 或 Link |

项目标签不再直接等同于 STAC Collection。Collection 应表达有共同空间时间范围、许可和产品语义的长期数据集。

### 8.2 STAC API

P1 引入 `pgSTAC + stac-fastapi`：

- `/stac/collections`
- `/stac/collections/{id}/items`
- `/stac/search`
- bbox、datetime、collections、ids、query 和 CQL2 过滤。
- Django 在网关层完成用户/组织授权，或为外部客户端发放 Token。
- QGIS、Python、Planetary Computer SDK 风格客户端可标准访问目录。

现有静态 `pystac` 文件继续生成，作为归档副本和离线交换格式。

## 9. 技术架构演进

### 9.1 数据库

当前 SQLite + DuckDB 适合 MVP，但不适合多用户目录、空间权限、版本、订阅和并发任务。

建议：

- PostgreSQL + PostGIS：权威业务库、空间查询、动态图层、权限关系。
- pgSTAC：STAC Item/Collection 的空间时间检索。
- DuckDB：保留为离线分析、批量导出和本地诊断，不再作为唯一在线检索库。
- Redis：任务队列、锁、短期状态和限流。
- OpenSearch：到数据量或长尾属性检索达到瓶颈后再引入，首期不必增加。

### 9.2 文件和对象存储

- 开发环境继续使用 `D:\code\airmap\data`。
- 生产使用 NAS 或 S3 兼容对象存储。
- 所有存储通过 `StorageBackend` 接口访问。
- COG、缩略图和导出包属于可重建派生资产；原始数据采用更严格保留策略。
- 大文件使用分片上传、断点续传和后台校验。

### 9.3 任务系统

- P0 保留现有 worker，同时统一任务和步骤模型。
- P1/P2 迁移到 Celery + Redis，Web 请求只创建任务。
- 归档解压、哈希、元数据、缩略图、COG 和发布分别幂等。
- 使用数据库唯一约束和幂等键处理重复提交，不只依赖前端文件名校验。

### 9.4 服务组件

- 栅格动态服务：TiTiler/Rio-Tiler。
- 多景目录：pgSTAC + TiTiler.PgSTAC 或 MosaicJSON。
- 矢量：PostGIS + pygeoapi/GeoServer；MVT 使用 Martin 或 pg_tileserv。
- WMS/WMTS：优先使用成熟 OGC 服务或网关适配，不手写完整协议。
- 前端：继续 React + Ant Design 原生组件 + Leaflet。
- 监控：Prometheus + Grafana + Loki/OpenSearch。

## 10. 分阶段研发路线

### Phase 0：领域模型地基与迁移

目标：不破坏现有功能，建立可扩展的数据对象模型。

- PostgreSQL/PostGIS 部署方案和迁移脚本。
- `DataObject` 与现有 `ImageryRecord` 一对一桥接。
- 统一任务/步骤/日志模型。
- 数据类型、生命周期、质量和可见性枚举。
- 现有影像、STAC、项目标签和服务回填。
- 全量备份、回滚和迁移核对工具。

验收：现有三大页面、影像接入、检索、JPG 预览和 XYZ 服务全部保持可用。

### Phase 1：数据空间、目录和元数据治理

- 个人/组织/共享数据空间。
- 虚拟目录、上传、下载、移动、复制、解压和回收站。
- 服务器目录登记和存储端点。
- 元数据 Schema、字段、分类树和标签。
- 文件名/XML/JSON 可配置提取规则。
- 批量元数据预览、编辑和重解析。
- 质量检查、冲突和缺失字段工作台。

验收：用户可以像网盘一样管理数据，同时数据对象不会因移动目录而重复复制。

### Phase 2：图层模型和高级检索

- 静态图层、动态规则图层和成员快照。
- 图层成员排序、启停、移除和地图预览。
- 多边形、行政区、上传矢量和空间关系查询。
- 保存检索方案。
- 多时相对比、卷帘和透明度。
- 数据对象、图层和服务的关联跳转。

验收：检索结果可生成图层，修改成员后不影响原始文件且服务 URL 不变。

### Phase 3：服务平台和 STAC API

- 多图层服务模型。
- 单景和多景动态栅格服务。
- WMS/WMTS 或 OGC API Tiles 兼容入口。
- STAC Collection 管理和 STAC API。
- 公共、登录、指定组织和 Token 访问。
- 服务版本、健康、预览、上下线和失败重试。
- QGIS/ArcGIS/Python 调用说明。

验收：QGIS 能通过标准入口加载服务，STAC 客户端能按 bbox/time 搜索数据。

### Phase 4：矢量、DEM 和结构化数据

- SHP/GeoJSON/KML/GPKG 导入和 PostGIS 编目。
- 矢量属性表、空间检索、GeoJSON、MVT 和 OGC API Features。
- DEM 类型、样式、预览和服务。
- CSV/XLSX 数据 Schema、导入、预览和基础 CRUD。
- 数据版本和血缘。

验收：影像、矢量和 DEM 可在同一目录、一张图和服务中心统一管理。

### Phase 5：订阅、分发和协作

- 保存查询转订阅。
- 入库事件匹配和定时扫描。
- 站内、邮件和 Webhook 通知。
- 数据篮、Manifest、STAC 导出、异步压缩和临时分享。
- 团队共享目录、评论或处理备注、负责人和审核状态。

验收：指定区域出现新影像后可自动通知，并能生成有审计记录的批量交付包。

### Phase 6：权限、计量和运营中心

- 组织/目录/对象/图层/服务权限。
- 空间、时间、属性范围策略。
- API Token、配额、限流和签名 URL。
- 使用事件、聚合统计和服务调用看板。
- Prometheus/Grafana、日志、告警和运行态势。
- 软删除、保留期、引用检查和物理清理流程。

验收：管理员能回答“谁在何时查看或下载了什么、哪个服务被调用多少次、哪个任务为何失败”。

### Phase 7：可选扩展

- WebDAV/SFTP 本地盘映射和桌面同步工具。
- 流式/轨迹、三维和多媒体深度管理。
- BPMN 可视化流程设计器。
- 商业订单、充值、支付和计费。
- 外部算法执行器和成果自动登记。

## 11. API 域划分建议

```text
/api/spaces
/api/directories
/api/files
/api/storage-endpoints
/api/data-objects
/api/metadata-schemas
/api/categories
/api/tags
/api/search
/api/layers
/api/services
/api/subscriptions
/api/baskets
/api/exports
/api/tasks
/api/usage
/api/audit
/stac
```

现有 `/api/imagery`、`/api/ingestion` 和 `/api/services` 保持兼容，在新模型稳定后逐步将前端切换到通用资源 API。

## 12. 跨模块关键业务流程

### 12.1 数据接入

```text
选择数据空间/目录
-> 上传、URL、目录登记或存储扫描
-> 前置重复提示
-> 后端哈希和业务标识幂等校验
-> 安全解压与文件组识别
-> 元数据方案匹配和提取
-> 用户预览/修正或自动入库
-> 质量检查
-> 创建 DataObject、资产、STAC 和索引
-> 可选加入图层/发布服务/触发订阅
```

前端文件名查重只用于快速提示，不能替代后端严格幂等和物理哈希判断。

### 12.2 图层动态更新

```text
编辑图层规则或成员
-> 预览新增/移除差异
-> 保存新图层版本
-> 刷新成员快照
-> 清理相关缓存
-> 对在线服务热更新
-> 记录审计和服务健康状态
```

### 12.3 数据订阅

```text
保存检索条件
-> 设定频率和通知方式
-> 新数据入库事件触发匹配
-> 与上次游标去重
-> 生成命中记录
-> 通知/加入数据篮/Webhook
-> 记录交付和访问审计
```

### 12.4 删除和清理

```text
用户删除逻辑引用
-> 进入回收站
-> 检查图层、服务、版本和分享引用
-> 保留期到期
-> 管理员审批或自动策略
-> 删除无引用派生资产
-> 最后删除无引用原始 Blob
-> 保留不可篡改审计摘要
```

## 13. 非功能要求

### 一致性

- 所有长任务幂等。
- 业务唯一性由数据库约束保证。
- 文件、数据库、STAC 和索引更新采用可补偿事务。
- 索引失败不能造成已入库数据永久不可见，应有重建命令。

### 性能

- 列表和空间查询使用服务端分页。
- 地图按视口、缩放级别和最大数量查询。
- 缩略图、TileJSON 和瓦片使用缓存和 ETag。
- 大文件不经过 Django 进程整包缓冲。
- 批量操作必须异步并显示可恢复进度。

### 安全

- 归档防路径穿越、压缩炸弹、符号链接和超限。
- URL 导入防 SSRF，仅允许批准的 HTTP/HTTPS 目的地策略。
- 存储路径、数据库 DSN 和对象存储凭据不返回前端。
- 下载和服务访问均做授权和审计。
- API Token 只展示一次，数据库保存哈希。

### 可维护性

- 数据类型解析器、存储后端、通知渠道和服务发布器均使用注册机制。
- 元数据规则配置化但需要版本、验证和测试，不能执行任意代码。
- 每个 Phase 必须补充迁移、回滚、API、操作手册和监控指标。

## 14. 总体验收清单

平台达到公开数据管理能力复刻目标时，至少应满足：

1. 用户能在个人或组织数据空间中完成文件目录管理和大文件汇聚。
2. 分散在本地、NAS 和对象存储的数据可在统一目录逻辑管理。
3. 影像、矢量和 DEM 能自动编目，元数据规则可配置、可批量修正、可追溯。
4. 数据分类树、标签和自定义元数据能驱动检索和动态图层。
5. 用户能通过属性、时间、矩形、多边形、行政区和上传范围检索数据。
6. 数据无需先建立业务服务即可在平台中预览。
7. 数据对象、图层、服务为三个独立层级，图层和服务不复制原始数据。
8. 图层成员可排序、筛选、移除和动态刷新，在线服务地址保持稳定。
9. 单景、多景、矢量和 DEM 可通过标准接口向 Web 和 QGIS 提供服务。
10. STAC API 可被标准客户端按空间和时间搜索。
11. 保存检索可以形成订阅，新数据命中后能自动通知和交付。
12. 下载、分享、服务调用和管理操作有完整权限、配额和审计。
13. 数据版本、质量、来源和派生关系可追溯。
14. 管理员能监控任务、服务、存储、用户访问和异常告警。
15. 算法成果可以登记和发布，但平台不依赖自研算法才能完成数据管理闭环。

## 15. 推荐的下一步

正式开发前先完成 Phase 0 的详细技术设计，重点决定：

1. SQLite/DuckDB 到 PostgreSQL/PostGIS 的迁移窗口和回滚方案。
2. `DataObject` 与现有 `ImageryRecord` 的兼容关系。
3. `DataLayer` 动态规则表达和成员快照机制。
4. 本地/NAS/S3 三种存储后端的统一对象接口。
5. STAC Collection 业务语义和 pgSTAC 引入方式。
6. QGIS 首期必须支持的协议是 WMS、WMTS、OGC API Tiles 还是组合。
7. 组织权限边界和现有“登录用户看全部影像”规则的迁移策略。

这些决策完成后，再把 Phase 0 和 Phase 1 拆为模型迁移、API、前端、任务、测试和数据迁移六类研发工作包。本文档本身不触发代码改动。

## 16. 资料引用

- 数简科技资源中心快速入门：<https://shujiantech.com/resources/?tab=quickstart>
- 数简时空数字基础平台：<https://shujiantech.com/products/spatiotemporal>
- 数简影像基础平台：<https://shujiantech.com/products/imagery>
- 数简矢量基础平台：<https://shujiantech.com/products/vector>
- 数简基础平台总览：<https://shujiantech.com/products/overview?target=structured-platform>
- 资源中心公开教程视频目录：<https://shujiantech.com/resources/?tab=videos>
- 快速入门引用的无人机数据文章：<https://mp.weixin.qq.com/s/BhEU7XxJPnReqRdCuHKlRQ>
- 快速入门引用的影像边界文章：<https://mp.weixin.qq.com/s/-ua3tz9VBuFuDT9mru5MEQ>
- 快速入门引用的自动化预处理文章：<https://mp.weixin.qq.com/s/ABYbS2EY6hm1d95Yp84R4Q>

